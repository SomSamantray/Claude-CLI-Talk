List all active CLI-Connect sessions on this machine.

Call MCP tool `cli_connect__list_sessions` and display the results as a table:
- Session name
- Active since
- Status (idle / processing)
- Number of pending messages
